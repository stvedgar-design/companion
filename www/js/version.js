// www/js/version.js
// Versión mostrada en Ajustes. Sin build/bundler no hay forma de inyectarla
// automáticamente desde package.json: se actualiza a mano acá (y en
// package.json, para que coincidan) en cada cambio que valga la pena que el
// usuario vea reflejado tras instalar un APK nuevo.
export const APP_VERSION = '1.1.0';
